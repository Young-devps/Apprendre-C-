#ifndef ENCAPSULATION_H
#define ENCAPSULATION_H


class Encapsulation
{
    public:
        Encapsulation();
        virtual ~Encapsulation();

    protected:

    private:
};

#endif // ENCAPSULATION_H
