#include <iostream>
using namespace std;

/*

struct nom{

    string nom;
    double taille;
    vector<int> cour;
    int age;
    //une variable de structure peut etre du type d'une autre stucture elle aussi contenant des variables de structure
}

void anniverssaire(personne& p) // pour puvoir modifier l'element de la structure
{
    ++(p.age);
}

int main()
{
    untel.taille = 1.75; // affecte a la valeur taille
    ++(untel.age); // ajoute un a lage de untel
    Personne P1 = {"Dupont", 1.75, 45}; // pour donner des valeurs
    //on affiche pas les valeur comme
    cout << p1; //pour le faire on peut ecrire une fonction qui recupere et affiche cella
    //autre chose on ne peut comparer p1==p2 par exemple, la comparaison et l'affichage se fait champs par champs



vector<personne> personnnes =
{
    {"Dupont", 1.75, 'M'},
    {"Jordy", 1.92, 'M'},
    {"Kylie", 1.65, 'F'}
}


}
*/
