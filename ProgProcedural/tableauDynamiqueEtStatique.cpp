#include <iostream>
using namespace std;
#include <vector>

/*
//Il existe deux type de tableau les vector dit dynamique et les arrays dit statique

vector<type> nomTab;
vector<type> nomTab = {val1, val2, val3};

for(size_t int i(0); i<12; ++i) //mettre & devant int si l'on veut modifier la valeur
{
}



for(auto element: tab)
{
    //on ne gre pas d'indexe dans ce cas si
    cout << element << endl;
}

array<type, taille> tab({les valeurs}); //tableau statique

typedef vector<double> Vecteur; //permet de donner un allias au type passev
typedef vecto<vector> Matrice;
Matrice rotation(3, Vecteur(3, 1.0)); //tableau dynamique de tableau dynamique en Realite vector<vector> rotation(3, Vecteur(3, 1.0));

*/
